# Required — server-to-server auth. Generate with: openssl rand -hex 32
ORVYRA_API_KEY=

# Optional — omit to run on heuristic fallbacks instead of real LLM reasoning
ANTHROPIC_API_KEY=

# Postgres connection, once storage/memory.py is migrated off in-process dict
DATABASE_URL=
